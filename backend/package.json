{
  "name": "hawk.admin",
  "description": "Hawk admin panel",
  "version": "0.0.0",
  "main": "src",
  "author": {
    "name": "CodeX team",
    "email": "team@codex.so"
  },
  "engines": {
    "node": "^11.0.0",
    "yarn": ">= 0.18.0"
  },
  "scripts": {
    "test": "yarn run compile && yarn run jest",
    "dev": "ts-node-dev --no-notify src/",
    "start": "yarn run compile && node dist/",
    "jest": "jest  --forceExit",
    "compile": "rm -rf lib/ && tsc"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "dotenv": "^8.1.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.17.1",
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "ts-node-dev": "^1.0.0-pre.42",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
